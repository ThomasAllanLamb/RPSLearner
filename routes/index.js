var express = require('express');
var router = express.Router();
var path = require('path');

/* GET home page. */
/* disabling for trying using express.static instead
router.get('/', function(req, res, next) {
  res.render('public.html');
});*/

module.exports = router;
